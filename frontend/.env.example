# Absolute URL of the backend API (including the /api prefix).
# Leave unset for local dev (requests go to the same origin via the Vite proxy).
# Example: VITE_API_URL=https://api.your-domain.com/api
VITE_API_URL=
